% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install_knhanes}
\alias{install_knhanes}
\title{安装或更新 knhanes}
\usage{
install_knhanes(
  license_code = NULL,
  version = "latest",
  force = FALSE,
  quiet = FALSE,
  lib = .libPaths()[1L]
)
}
\arguments{
\item{license_code}{首次激活使用的完整knhanes授权码，新版通常以\code{KNHLIC3.}开头。
旧\code{KNHLIC1.}和\code{KNHLIC2.}授权码仍可使用。
已激活用户更新时设为\code{NULL}即可。}

\item{version}{要安装的版本。默认\code{"latest"}安装最新正式Release；也可指定
如\code{"0.1.0.4"}或\code{"v0.1.0.4"}。}

\item{force}{逻辑值。若为\code{TRUE}，即使同版本已经安装也重新安装。}

\item{quiet}{逻辑值。是否减少下载和安装过程输出。}

\item{lib}{安装R包的库目录。默认使用\code{.libPaths()}中的第一个目录。}
}
\value{
隐式返回\code{\link[=license_status]{license_status()}}的一行tibble。
}
\description{
从维护者发布仓库下载指定knhanes版本，验证SHA-256与Ed25519发布签名后安装。
首次安装可同时提供\code{KNHLIC3}授权码完成激活；老用户更新时无需重复输入授权码。
}
\details{
发布包、校验文件和签名来自\code{heng8886/knhanes-dist}的版本化GitHub Release。
本函数不需要GitHub账号或PAT。校验通过后会读取发布包的\code{DESCRIPTION}，
使用当前CRAN镜像自动安装尚未安装的\code{Depends}、\code{Imports}和\code{LinkingTo}依赖，
然后才安装knhanes；若当前未配置CRAN镜像，则使用
\verb{https://cloud.r-project.org}。因此Windows和macOS用户通常无需预先手工安装依赖。
授权保存在R用户配置目录，正常更新不会删除。若knhanes已经在当前R会话加载，
更新后建议重启R再使用新版本。
}
\examples{
\dontrun{
install_knhanes(license_code = "KNHLIC3.<payload>.<signature>")
install_knhanes()
}
}
